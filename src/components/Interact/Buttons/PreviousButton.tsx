import React from 'react'
import * as S from '../Panels/Playpanel.styles'

export default function PreviousButton() {
    const handleClick = () => {}
    return (
        <div>
            <S.PreviousButton onClick={handleClick}></S.PreviousButton>
        </div>
    )
}
